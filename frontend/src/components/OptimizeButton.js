// src/components/OptimizeButton.js
import React from 'react';

function OptimizeButton({ onClick }) {
    return (
        <button onClick={onClick}>Optimize Portfolio</button>
    );
}

export default OptimizeButton;
